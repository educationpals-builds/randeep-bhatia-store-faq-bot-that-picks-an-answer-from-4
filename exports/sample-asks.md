# Sample asks

A STRANGER OPENS IT — paste a failing setup and ask for a scored audit. Sub-bullets are notes — not the paste.

They enter (authored): A stranger describes the failing setup they rely on — what it is supposed to do, who gets hurt when it fails, and a few real failing inputs. The tool walks five checks conversationally, proposes findings with the measurement that would confirm each, and returns a scored audit with a severity story, a call, and a tripwire. Instantiate every example from the builder's specimen — never leave a pack demo domain in the shipped files.

1. My failing setup: Your ticket bot tags the wrong issue when a customer says "it broke again". The bot opens a new ticket instead of finding the old one. Run the five checks. For each check give a finding plus the measurement that would confirm it. End with severity, call, and tripwire.
   - Role: stranger
   - Persona: Support ops lead
   - Situation: Ticket bot loses track of "it"
   - Expect: Scored per-check findings with measurements, a severity story, a call, and a tripwire — no coach questions.

2. My failing setup: A lease tool is supposed to split contract lines into separate duties. On lines with "provided that", it still merges two duties so the wrong party looks responsible. Run the five checks. For each check give a finding plus the measurement that would confirm it. End with severity, call, and tripwire.
   - Role: stranger
   - Persona: Legal ops analyst
   - Situation: Lease tool mixes two duties
   - Expect: Scored per-check findings with measurements, a severity story, a call, and a tripwire — no coach questions.

3. My failing setup: Kaveh faces the board in ten days. He wants a plain answer first: when customers say "it", are the routing parts still losing the old ticket? Run the five checks. For each check give a finding plus the measurement that would confirm it. End with severity, call, and tripwire.
   - Role: stranger
   - Persona: Support ops lead
   - Situation: Ticket bot, board demo in ten days
   - Expect: Scored per-check findings with measurements, a severity story, a call, and a tripwire — no coach questions.

## How to score a try

- stranger: shell product audits their failing setup (not coaching them).
- Expect scored checks + severity + call + tripwire — never a coach question.
